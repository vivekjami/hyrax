# SigNoz Foundry casting file
# Install foundryctl: curl -fsSL https://signoz.io/foundry.sh | bash
# Deploy:            foundryctl cast -f casting.yaml
# SigNoz UI:         http://localhost:8080
# OTLP (gRPC):       localhost:4317
# OTLP (HTTP):       localhost:4318

apiVersion: v1alpha1
kind: Installation
metadata:
  name: signoz
spec:
  deployment:
    flavor: compose
    mode: docker
